version: '3.1'

services:
  api:
    build:
      context: .
    command: 'bash -c "./gradlew bootRun"'
    working_dir: '/app'
    depends_on:
      - mongo
    volumes:
      - '.:/app'

  mongo:
      image: mongo
      restart: always
      environment:
        MONGO_INITDB_ROOT_USERNAME: root
        MONGO_INITDB_ROOT_PASSWORD: example
